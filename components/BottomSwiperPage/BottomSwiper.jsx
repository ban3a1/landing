import React from "react";
import Swiper from "./Swiper";

export default function BottomSwiper({ width }) {
  return (
    <div className="bottom-swiper-page">
      <Swiper width={width} />
    </div>
  );
}
